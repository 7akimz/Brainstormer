.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", team_events_path
      %li= link_to "#{t("web-app-theme.new", :default => "New")}", new_team_event_path
      %li.active= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_team_event_path
  .content
    %h2.title
      = t("web-app-theme.edit", :default => "Edit")
      Event
    .inner
      = form_for @event, :url => team_event_path(@event), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
