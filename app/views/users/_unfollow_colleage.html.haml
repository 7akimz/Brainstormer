= form_for current_user.relationships.find_by_followed_id(@user), |
:html => { :method => :delete }, :remote => true do |f| |
  %button.button{:type => "submit"}
    = image_tag("web-app-theme/icons/cross.png", :alt => "#{t("web-app-theme.delete", :default => "Unfollow Colleage")}")
    = t("web-app-theme.delete", :default => "Unfollow Colleage")
