= form_for current_user.relationships.build(:followed_id => |
  @user.id), :remote => true do |f| |
  = f.hidden_field :followed_id
  %button.button{:type => "submit"}
    = image_tag("web-app-theme/icons/tick.png", :alt => "#{t("web-app-theme.save", :default => "Follow Colleage")}")
    = t("web-app-theme.save", :default => "Follow Colleage")
